# Local PostgreSQL (Homebrew) — uses your OS username by default
# DATABASE_URL=postgresql+psycopg://your_user@localhost:5432/cafe_fausse

# Docker Compose PostgreSQL
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/cafe_fausse
